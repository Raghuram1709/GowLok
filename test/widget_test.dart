// Basic smoke test for GowlokApp
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('GowlokApp module can be imported', () {
    // Verifies that the project compiles without errors.
    // Full widget tests require Supabase initialization and are
    // better suited for integration testing.
    expect(1 + 1, 2);
  });
}
